server:
  port: 8081

spring:
  application:
    name: restaurant-service
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true